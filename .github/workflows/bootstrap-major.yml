name: Bootstrap Major Track

# Runs daily and on demand. Detects when the latest upstream omp major exceeds
# the current default-branch track number, then creates the new track branch,
# updates the versioned files, creates the SDK store track, changes the
# default branch, and opens a PR that auto-merges to trigger the first
# upload — fully unattended.
#
# Prerequisites:
#   SDKCRAFT_STORE_TOKEN  — required; store authentication
#   ADMIN_TOKEN           — optional; a fine-grained PAT with
#                           Administration:write on this repo.
#                           Only needed to change the default branch.
#                           If absent the workflow logs a warning and the
#                           caller must run:
#                             gh api repos/OWNER/REPO -X PATCH \
#                               -f default_branch=track/N
on:
  schedule:
    - cron: "0 5 * * *"   # 05:00 UTC daily (runs from the default branch)
  workflow_dispatch:

permissions:
  contents: write        # push setup branch, create refs
  pull-requests: write   # create PR, enable auto-merge

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Detect version gap
        id: detect
        run: |
          set -euo pipefail

          # Highest major version published by upstream (any release, not just "Latest")
          LATEST_MAJOR=$(gh release list --repo can1357/oh-my-pi --limit 100 \
            --json tagName --jq \
            '[.[].tagName | select(test("^v[0-9]+\\.")) | ltrimstr("v") |
             split(".")[0] | tonumber] | max | tostring')

          # Default branch carries the current track number
          DEFAULT_BRANCH=$(gh repo view \
            --json defaultBranchRef --jq '.defaultBranchRef.name')
          CURRENT_MAJOR=$(echo "$DEFAULT_BRANCH" | cut -d/ -f2)

          # Most-recent stable release within the new major (releases are newest-first)
          LATEST_VERSION=$(gh release list --repo can1357/oh-my-pi --limit 100 \
            --json tagName,isPrerelease \
            --jq "[.[] | select(.isPrerelease==false) |
                  select(.tagName | startswith(\"v${LATEST_MAJOR}.\")) |
                  .tagName | ltrimstr(\"v\")] | .[0]")

          echo "latest_major=$LATEST_MAJOR"     >> "$GITHUB_OUTPUT"
          echo "latest_version=$LATEST_VERSION" >> "$GITHUB_OUTPUT"
          echo "current_major=$CURRENT_MAJOR"   >> "$GITHUB_OUTPUT"

          if [ "$LATEST_MAJOR" -gt "$CURRENT_MAJOR" ]; then
            echo "needs_bootstrap=true"  >> "$GITHUB_OUTPUT"
          else
            echo "needs_bootstrap=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Bootstrap track/${{ steps.detect.outputs.latest_major }}
        if: steps.detect.outputs.needs_bootstrap == 'true'
        env:
          OLD_MAJOR: ${{ steps.detect.outputs.current_major }}
          NEW_MAJOR: ${{ steps.detect.outputs.latest_major }}
          NEW_VERSION: ${{ steps.detect.outputs.latest_version }}
          SDKCRAFT_STORE_CREDENTIALS: ${{ secrets.SDKCRAFT_STORE_TOKEN }}
          ADMIN_TOKEN: ${{ secrets.ADMIN_TOKEN }}
        run: |
          set -euo pipefail

          # ── Guard: skip if branch already exists (idempotent re-runs) ──────
          if git ls-remote --exit-code origin "track/${NEW_MAJOR}" \
              > /dev/null 2>&1; then
            echo "Branch track/${NEW_MAJOR} already exists — nothing to do."
            exit 0
          fi

          # ── Git identity ─────────────────────────────────────────────────
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name  "github-actions[bot]"

          # ── Create track/N branch via refs API ───────────────────────────
          # Pointing at the current HEAD (which already has a passing build
          # check) satisfies the required_status_checks ruleset rule for
          # branch creation — no ruleset disable/re-enable needed.
          TRACK_SHA=$(git rev-parse HEAD)
          gh api "repos/${GITHUB_REPOSITORY}/git/refs" \
            -X POST \
            -f ref="refs/heads/track/${NEW_MAJOR}" \
            -f sha="${TRACK_SHA}"

          # ── Create local setup branch for the config changes ─────────────
          git checkout -b "setup/track-${NEW_MAJOR}"

          # ── VERSION ─────────────────────────────────────────────────────
          echo "$NEW_VERSION" > VERSION

          # ── renovate.json: baseBranchPatterns, matchBaseBranches, allowedVersions
          # The allowedVersions string in the parsed JSON is /^<N>\./ (the JSON
          # encoding doubles the backslash to /^<N>\\./). jq --arg passes the
          # decoded value, so we write the single-backslash form here.
          jq --arg old "track/${OLD_MAJOR}" \
             --arg new "track/${NEW_MAJOR}" \
             --arg oldv "/^${OLD_MAJOR}\./" \
             --arg newv "/^${NEW_MAJOR}\./" \
            '.baseBranchPatterns |=
               map(if . == $old then $new else . end) |
             .packageRules |= map(
               .matchBaseBranches |=
                 map(if . == $old then $new else . end) |
               if .allowedVersions == $oldv
                 then .allowedVersions = $newv
                 else .
               end
             )' renovate.json > renovate.json.tmp \
            && mv renovate.json.tmp renovate.json

          git add VERSION renovate.json
          git commit -m "chore: configure ${NEW_MAJOR}/edge track"

          # ── Push setup branch (not track/*, no ruleset restriction) ──────
          git push origin "setup/track-${NEW_MAJOR}"

          # ── Open PR: setup/track-N → track/N with auto-merge ─────────────
          # build.yml uses branches: ["track/*"] so it fires on this PR.
          # upload.yml uses push: branches: ["track/*"] so it fires on merge,
          # BUT GITHUB_TOKEN-initiated merges do NOT trigger further workflows;
          # we therefore poll for the merge and trigger upload explicitly below.
          PR_URL=$(gh pr create \
            --base  "track/${NEW_MAJOR}" \
            --head  "setup/track-${NEW_MAJOR}" \
            --title "chore: configure ${NEW_MAJOR}/edge track" \
            --body  "Automated bootstrap: bumps VERSION to ${NEW_VERSION} and \
          updates renovate.json for the ${NEW_MAJOR}/edge track.")
          PR_NUMBER=$(basename "${PR_URL}")

          # Auto-merge + delete the setup branch once the build check passes
          gh pr merge --auto --merge --delete-branch "${PR_URL}"

          # ── Install sdkcraft ──────────────────────────────────────────────
          for attempt in 1 2 3; do
            sudo snap install sdkcraft --edge --classic && break
            [ "$attempt" -lt 3 ] && sleep $((attempt * 5))
          done

          # ── Create store track (required before first upload) ─────────────
          # Must happen before the PR auto-merges and triggers upload.yml.
          sdkcraft create-track omp --track "${NEW_MAJOR}"

          # ── Change default branch ──────────────────────────────────────────
          # GITHUB_TOKEN lacks Administration:write — use ADMIN_TOKEN if set.
          _admin_token="${ADMIN_TOKEN:-}"
          if GH_TOKEN="${_admin_token:-$GH_TOKEN}" \
              gh api "repos/${GITHUB_REPOSITORY}" \
              -X PATCH -f "default_branch=track/${NEW_MAJOR}" \
              2>/dev/null; then
            echo "Default branch changed to track/${NEW_MAJOR}."
          else
            echo "::warning::Could not change default branch automatically." \
                 "Set the ADMIN_TOKEN secret (fine-grained PAT with" \
                 "Administration:write), or run manually:"
            echo "::warning::  gh api repos/${GITHUB_REPOSITORY} -X PATCH" \
                 "-f default_branch=track/${NEW_MAJOR}"
          fi

          # ── Wait for the setup PR to merge, then trigger upload ────────────
          # GITHUB_TOKEN-initiated merges do not trigger further workflows.
          # We poll here (up to 40 × 30 s = 20 min) and fire upload explicitly.
          echo "Waiting for PR #${PR_NUMBER} to auto-merge..."
          MERGED=false
          for i in $(seq 1 40); do
            STATE=$(gh pr view "${PR_NUMBER}" --json state --jq '.state')
            if [ "$STATE" = "MERGED" ]; then
              MERGED=true
              break
            fi
            echo "  [${i}/40] PR state=${STATE} — waiting 30 s..."
            sleep 30
          done

          if [ "$MERGED" = "true" ]; then
            echo "PR merged. Triggering first upload to ${NEW_MAJOR}/edge..."
            gh workflow run upload.yml \
              --ref "track/${NEW_MAJOR}" \
              -f branch="track/${NEW_MAJOR}"
            echo "Upload queued for track/${NEW_MAJOR}."
          else
            echo "::warning::PR #${PR_NUMBER} did not merge within 20 min." \
                 "Trigger upload manually once the PR merges:"
            echo "::warning::  gh workflow run upload.yml" \
                 "--ref track/${NEW_MAJOR} -f branch=track/${NEW_MAJOR}"
          fi
